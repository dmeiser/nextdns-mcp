name: CodeQL

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: '23 3 * * 1'

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: actions
            build-mode: none
          - language: python
            build-mode: none

    steps:
      - name: Checkout
        uses: actions/checkout@v4@11d5960a326750d5838078e36cf38b85af677262  # actions/checkout@v4

      - name: Set up Python
        if: matrix.language == 'python'
        uses: actions/setup-python@v6@ece7cb06caefa5fff74198d8649806c4678c61a1  # actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3@87ef0dc97def48aa960fbf026a2563ee9dbdb470  # github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      - name: Perform CodeQL analysis
        uses: github/codeql-action/analyze@v3@87ef0dc97def48aa960fbf026a2563ee9dbdb470  # github/codeql-action/analyze@v3
        with:
          category: /language:${{ matrix.language }}